1. Clone
git clone https://github.com/YataoWang/Tools.git

2. Remove remote file and keep local file
git rm -f -r -cached ....

3. Remove remote and local file
git rm -f -r -cached ....

4. Rename file  
git mv -f oldfile newfile  
git add -u newfolder

5. Update
git poll origin master

6. Commit
git add "**************"
git commit -m ""
8it push origin master

7. Config the Git user
git config --global user.name ""
git config --global user.email ""

9. Save the passsword
git config --global credential.helper store
